const INITIAL_STATE = {
  operatoLogis: {
    baseUrl: null
  }
}

const operatoLogis = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default: return state
  }
}

export default operatoLogis