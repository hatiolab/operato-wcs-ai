# 작업 로그 — 2026-03-04

## 19:54 — 백엔드 서버 실행 환경 구성 및 스킬 추가

### 수행 내용
- **mainClass 수정**: `build.gradle`의 mainClass를 `xyz.anythings.boot.AnythingsBootApplication` → `operato.logis.OperatoWcsBootApplication`으로 변경 (사용자가 새 Application 클래스 생성)
- **누락 properties 파일 생성**: `anythings-comm-rabbitmq.properties` 생성 (서버 시작 시 FileNotFoundException 해결)
- **DB 스키마 수정**: PostgreSQL `domains` 테이블에 누락 컬럼 추가
  - `parent_id`, `attributes`, `iplist`, `deleted_at` 4개 컬럼 추가
  - `ext_type`, `owner`는 이미 존재
  - psql 설치 (`brew install libpq`) 후 원격 DB에 직접 ALTER TABLE 실행
- **서버 실행 성공**: dev 프로파일로 서버 정상 기동 확인
  - URL: http://localhost:9500, 시작 시간 약 31초
  - HTTP 200 응답 확인
- **stop-backend 스킬 생성**: 백엔드 서버 중지 자동화 스킬 등록
  - `.claude/skills/stop-backend/SKILL.md` 생성
  - README.md, CLAUDE.md 스킬 목록 업데이트

### 변경 파일
- `build.gradle` — mainClass를 `operato.logis.OperatoWcsBootApplication`으로 변경
- `src/main/resources/properties/anythings-comm-rabbitmq.properties` — 생성 (RabbitMQ 모듈 설정)
- `.claude/skills/stop-backend/SKILL.md` — 생성 (서버 중지 스킬)
- `.claude/skills/README.md` — stop-backend 스킬 추가, 번호 재정렬
- `CLAUDE.md` — 사용 가능한 스킬 목록에 stop-backend 추가

### 비고
- **서버 실행 시 해결한 오류 순서**:
  1. `ClassNotFoundException: AnythingsBootApplication` → mainClass 변경
  2. `FileNotFoundException: anythings-comm-rabbitmq.properties` → 파일 생성
  3. `Driver MySQL vs URL PostgreSQL 불일치` → 사용자가 직접 수정
  4. `Circular reference (logisServiceDispatcher ↔ baseSkuSearchService)` → 사용자가 `spring.main.allow-circular-references=true` 추가
  5. `Couldn't find column[parent_id] of table[domains]` → DB ALTER TABLE로 해결
- **DB 접속 도구**: `/opt/homebrew/opt/libpq/bin/psql` 설치 완료 (PostgreSQL 클라이언트)
- **현재 스킬 총 6개**: commit, deploy-backend, run-backend, stop-backend, docker-backend, log

## 21:07 — 백엔드 빌드 및 서버 실행·중지

### 수행 내용
- **백엔드 빌드 (`/deploy-backend`)**: Java 18로 전환 후 clean build 성공 (테스트 스킵, 6초 소요)
  - `operato-wcs-ai.jar` 169MB, `operato-wcs-ai-plain.jar` 17MB 생성
  - 경고 1건: `StageJobConfigUtil.java:459` — deprecated 항목 `@Deprecated` 미지정 (경미)
- **백엔드 서버 실행 (`/run-backend --profile=dev`)**: dev 프로파일로 서버 정상 기동
  - 포트 9500, PID 31908, 시작 시간 4.5초
  - 모든 모듈 초기화 완료 (Core, Msg, Sec, Sys, MQBase, BMS, DAS, DPC, DPS, WCS 등)
  - HTTP 200 응답 확인
- **백엔드 서버 중지 (`/stop-backend`)**: PID 31908 정상 종료, 포트 9500 해제 완료

### 비고
- 이전 세션(19:54) 대비 서버 시작 시간 크게 단축 (31초 → 4.5초) — Gradle 캐시 및 빌드 캐시 효과
- 컴파일 에러 없이 빌드 성공 (이전에 발생했던 `xyz.elidom.rabbitmq` 관련 에러 해소됨)
