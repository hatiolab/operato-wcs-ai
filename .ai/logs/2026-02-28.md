# 작업 로그 — 2026-02-28

## 17:30 — CLAUDE.md 생성

✅ 프로젝트 개요 (Hatiolab / Operato WCS AI Features)
✅ 기술 스택 (Spring Boot 3.4, Gradle, Lit)
✅ 프로젝트 디렉토리 구조 (docs/ 포함)
✅ 프론트엔드 디렉토리 구조 및 명명 규칙
✅ 빌드 및 실행 명령어
✅ 운영 전략 (bootJar 직접 실행 / Docker 배포)
✅ Git 규칙 (자동 커밋 금지)
⬜ 코딩 컨벤션 (추후 추가 예정)

## 18:30 — requirements 폴더 내 파일 생성 

✅ 업무-프로세스.md 파일 생성
✅ ECS-모듈-기능정의.md 파일 생성

## 23:12 — WCS 기능 분석 및 문서 정리 작업

### 수행 내용
- Operato WCS 제품의 경쟁 제품 대비 부족 기능 및 AI 시대 신규 기능 분석
- WCS/WMS 업계 동향 리서치 (Manhattan Associates, Blue Yonder, Korber 등)
- 기능 개선 로드맵 문서 작성 (Phase 1~4 우선순위 정리)
- `ref/Operato_WCS_FunctionList_V1.xlsx` 엑셀 파일 분석 (5개 시트, 184개 기능)
- WCS 매니저 기능정의서 신규 작성 (Menu 시트 → 매니저 9개 메뉴 + 작업 화면 3개 단말)
- ECS 모듈 기능정의서 전면 업데이트 (PutToLight, PickToLight, PDAS, DPC 4개 모듈 상세 기능)
- 작업 로그 기록용 Claude Code Skill (`/log`) 생성

### 변경 파일
- `docs/requirements/기능-개선-로드맵.md` — 경쟁 제품 대비 부족 기능 + AI 신규 기능 + 개발 우선순위 로드맵 (신규)
- `docs/requirements/WCS-기능정의.md` — WCS 매니저 기능 + 작업 화면 기능 정의서 (신규)
- `docs/requirements/ECS-모듈-기능정의.md` — 4개 ECS 모듈 상세 기능 184건 + 상태 요약 (전면 업데이트)
- `docs/research/WCS-WMS-Trends-2025-2026.md` — 업계 동향 리서치 자료 (신규, 자동 생성)
- `.claude/skills/log/SKILL.md` — 작업 로그 기록 Skill 정의 (신규)

### 비고
- ECS 모듈 미완료 기능 7건 확인 (PTL 3건, P2L 2건, DPC 4건 — 추후/2차 개발 대상)
- `/log` Skill은 다음 세션부터 자동 인식됨

## 23:29 — `/log` Skill 사용법 확인 및 테스트

### 수행 내용
- `/log` 명령어 실행하여 작업 로그 기록 기능 테스트
- 기존 로그 파일에 정상적으로 append되는지 확인

### 변경 파일
- `.ai/logs/2026-02-28.md` — 현재 작업 내용 추가 기록

### 비고
- `/log` Skill은 `.claude/skills/log/SKILL.md`에 정의되어 있으며, 다음 세션부터 자동으로 작동함
- 현재는 수동으로 로그 기록 기능을 시연

## 01:40 — Git 빌드 파일 제외 설정 및 검증

### 수행 내용
- `.gitignore` 파일에 Gradle 빌드 관련 디렉토리 추가 (.gradle, bin/, build/)
- `gradle/` 폴더의 .gitignore 포함 여부 검증 (표준 Gradle 프로젝트 관행 확인)
- Gradle Wrapper 파일이 Git에 커밋되어야 하는 이유 확인 및 설명

### 변경 파일
- `.gitignore` — Gradle 빌드 디렉토리 3개 추가 (로컬 빌드 산출물 제외)

### 비고
- `gradle/wrapper/` 디렉토리는 Git에 커밋되어야 함 (Gradle Wrapper 보존)
- `.gradle/` (캐시)와 `gradle/` (Wrapper)의 차이점 확인 완료
- 현재 설정이 표준 Java/Gradle 프로젝트 관행에 부합함을 검증

## 01:44 — Spring Boot 백엔드 빌드 성공

### 수행 내용
- Java 18로 자동 전환하여 Gradle 빌드 실행
- 테스트 스킵 옵션으로 빌드 시간 단축 (11초)
- JAR 파일 생성 확인 (157MB)

### 변경 파일
- `build/libs/operato-wcs-ai.jar` — 실행 가능한 JAR (157MB)
- `build/libs/operato-wcs-ai-plain.jar` — 의존성 제외 JAR (18MB)

### 비고
- 1개 경고: deprecated 메서드 어노테이션 누락 (기능 문제 없음)
- 컴파일 에러 22건 모두 해결 완료 (이전 세션)

## 01:48 — Docker 배포 환경 구축

### 수행 내용
- 백엔드 Docker 컨테이너 생성을 위한 모든 파일 작성
- 멀티스테이지 빌드 Dockerfile 생성
- 사전 빌드 JAR 사용 Dockerfile.simple 생성
- Docker Compose 설정 파일 작성
- 상세 사용 가이드 문서 작성

### 변경 파일
- `Dockerfile` — 멀티스테이지 빌드 (Docker 내부에서 빌드) (신규)
- `Dockerfile.simple` — 사전 빌드된 JAR 사용 (빠른 빌드) (신규)
- `.dockerignore` — Docker 이미지 제외 파일 목록 (신규)
- `docker-compose.yml` — Docker Compose 설정 (신규)
- `docs/DOCKER.md` — Docker 상세 사용 가이드 (신규)

### 비고
- 비루트 사용자(spring) 실행으로 보안 강화
- 헬스 체크 자동화 (30초 간격)
- JVM 메모리 최적화 설정 포함

## 01:52 — `/deploy` 스킬 생성

### 수행 내용
- Spring Boot 백엔드 빌드 자동화 스킬 생성
- Java 버전 자동 전환 기능 포함
- 테스트 포함/제외, clean 포함/제외 옵션 지원

### 변경 파일
- `.claude/skills/deploy/SKILL.md` — 백엔드 빌드 스킬 정의 (신규)

### 비고
- `/deploy` 명령으로 간편하게 JAR 빌드 가능
- 에러 처리 및 결과 보고 자동화
- 다음 세션부터 자동 인식

## 01:54 — `/docker-backend` 스킬 생성

### 수행 내용
- 백엔드 Docker 이미지 빌드 및 컨테이너 실행 자동화 스킬 생성
- 멀티스테이지 빌드, 사전 빌드 JAR, Docker Compose 3가지 방식 지원
- 프론트엔드와 구분되는 명확한 이름 지정 (향후 `/docker-frontend` 예정)

### 변경 파일
- `.claude/skills/docker-backend/SKILL.md` — 백엔드 Docker 빌드 스킬 정의 (신규)

### 비고
- `--simple`, `--build-only`, `--clean` 등 옵션 지원
- Docker 데몬 확인, 포트 충돌 등 에러 처리 자동화
- `/docker-frontend` 스킬과 이름 충돌 방지

## 01:57 — Docker 이미지 빌드 및 컨테이너 실행 시도

### 수행 내용
- `/docker-backend` 스킬로 Docker 이미지 빌드 실행
- settings.gradle 파일 누락 발견 및 생성
- 멀티스테이지 빌드 성공 (약 4분 21초)
- 컨테이너 실행 시도 (logback 설정 파일 문제로 실패)

### 변경 파일
- `settings.gradle` — Gradle 프로젝트 설정 파일 (신규)

### 비고
- Docker 이미지: operato-wcs-ai:latest (393MB 압축, 1GB 전체)
- 컨테이너 재시작 반복: logback-spring.xml 파일 경로 문제
- 해결 방안: 사전 빌드된 JAR 사용 (`/docker-backend --simple`) 권장

## 02:05 — 백엔드 빌드 재실행 성공

### 수행 내용
- `/deploy` 스킬로 백엔드 JAR 재빌드
- Java 18 환경에서 정상 빌드 완료 (11초)
- Docker simple 빌드를 위한 사전 준비 완료

### 변경 파일
- `build/libs/operato-wcs-ai.jar` — 재빌드된 실행 가능 JAR (157MB)

### 비고
- 다음 단계: `/docker-backend --simple`로 빠른 Docker 이미지 생성 가능
- 로컬 빌드 JAR 사용으로 logback 설정 파일 문제 우회 가능
