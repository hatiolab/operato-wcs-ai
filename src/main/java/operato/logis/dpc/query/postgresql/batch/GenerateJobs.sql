INSERT INTO JOB_INSTANCES (
	ID,
	BATCH_ID,
	JOB_DATE,
	JOB_SEQ,
	BIZ_TYPE,
	JOB_TYPE,
	STAGE_CD,
	COM_CD,
	EQUIP_TYPE,
	EQUIP_GROUP_CD,
	EQUIP_CD,
	SUB_EQUIP_CD,
	CUST_ORDER_NO,
	ORDER_NO,
	CLASS_CD,
	INVOICE_ID,
	BOX_TYPE_CD,
	ORDERER_ID,
	ORDERER_NM,
	SHOP_CD,
	SHOP_NM,
	SKU_CD,
	SKU_BARCD,
	SKU_NM,
	INPUT_SEQ,
	BOX_IN_QTY,
	PICK_QTY,
	PICKING_QTY,
	PICKED_QTY,
	INSPECTED_QTY,
	ORDER_TYPE,
	DOMAIN_ID,
	CREATOR_ID,
	UPDATER_ID,
	CREATED_AT,
	UPDATED_AT
)
SELECT
	uuid_generate_v4()::text ID,
	BATCH_ID,
	JOB_DATE,
	JOB_SEQ,
	BIZ_TYPE,
	JOB_TYPE,
	STAGE_CD,
	COM_CD,
	EQUIP_TYPE,
	EQUIP_GROUP_CD,
	EQUIP_CD,
	SUB_EQUIP_CD,
	CUST_ORDER_NO,
	ORDER_NO,
	CLASS_CD,
	INVOICE_ID,
	MAX(BOX_TYPE_CD) AS BOX_TYPE_CD,
	MAX(ORDERER_ID) AS ORDERER_ID,
	MAX(ORDERER_NM) AS ORDERER_NM,
	MAX(SHOP_CD) AS SHOP_CD,
	MAX(SHOP_NM) AS SHOP_NM,
	SKU_CD,
	MAX(SKU_BARCD) AS SKU_BARCD,
	MAX(SKU_NM) AS SKU_NM,
	0 AS INPUT_SEQ,
	MAX(BOX_IN_QTY) AS BOX_IN_QTY,
	SUM(ORDER_QTY) AS PICK_QTY,
	0 AS PICKING_QTY,
	0 AS PICKED_QTY,
	0 AS INSPECTED_QTY,
	MAX(ORDER_TYPE) AS ORDER_TYPE,
	DOMAIN_ID,
	'system',
	'system',
	now(),
	now()
FROM
	ORDERS
WHERE
	DOMAIN_ID = :domainId
	AND BATCH_ID  = :batchId
GROUP BY
	DOMAIN_ID,
	BATCH_ID,
	JOB_DATE,
	JOB_SEQ,
	BIZ_TYPE,
	JOB_TYPE,
	STAGE_CD,
	COM_CD,
	EQUIP_TYPE,
	EQUIP_GROUP_CD,
	EQUIP_CD,
	SUB_EQUIP_CD,
	CUST_ORDER_NO,
	ORDER_NO,
	CLASS_CD,
	INVOICE_ID,
	SKU_CD